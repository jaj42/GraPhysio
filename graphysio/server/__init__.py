"""Qt-free FastAPI backend for the GraPhysio web frontend.

Like ``graphysio.core``, nothing in this package may import a GUI toolkit. The
server reuses the pure-pandas analytical core and exposes it over HTTP.
"""
